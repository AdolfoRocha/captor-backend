version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - DATABASE_URL=file:./dev.db
    volumes:
      - ./server/prisma/dev.db:/app/server/prisma/dev.db
