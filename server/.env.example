# Use your PostgreSQL connection string
# For local development, you can use Docker:
# docker run --name captor-db -e POSTGRES_PASSWORD=captor -e POSTGRES_DB=captor -p 5432:5432 -d postgres

DATABASE_URL="postgresql://postgres:captor@localhost:5432/captor"
